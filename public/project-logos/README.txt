Drop project logos here as PNG files named by project id:
  alpha.png, phoenix.png, atlas.png, sentinel.png, compass.png, beacon.png

Square images (e.g. 128x128) look best. If no file exists for a project,
the portal shows a colored monogram with the project's initials instead.
